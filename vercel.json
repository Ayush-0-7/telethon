{
    "builds":[
        {
            "src":"api/keep_alive.py",
            "use":"@vercel/python"
        }
    ],
    "routes":[
        {
            "src":"/(.*)",
            "dest":"api/keep_alive.py"
        }
    ]
}